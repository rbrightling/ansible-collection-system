---
name: "rbrightling.system.ansible_node"

on: 
  push:
    paths:
      - 'roles/ansible_node/**'
      - 'molecule/ansible_node/**'
      - '.github/workflows/ansible_node.yml'

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - name: Ansible Lint
       uses: ansible/ansible-lint-action@master
       with:
         targets: "roles/ansible_node"

  test:
    name: molecule
    needs: lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ansible:
          - ansible210
        distro:
          - debian10
          - centos8
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.7'
          architecture: 'x64'
      - name: install tox
        run: |
          python -m pip install --upgrade pip
          pip install tox
      - name: run tox tests
        run: tox -e ${{ matrix.ansible }}-ansible_node-${{ matrix.distro }}
        env:
          PY_COLORS: '1'
          ANSIBLE_FORCE_COLOR: '1'
          MOLECULE_DRIVER: 'docker'
